describe("ComponentMap", function() {
    it("counts the number of connected components in a grid", function() {
        //var connComp = new ComponentMap([], 0);
        //expect(connComp.numberComponents).toBe(0);
    });
});

